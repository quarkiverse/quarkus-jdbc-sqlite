---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: "SQLite4J - JDBC Driver"
metadata:
  keywords:
    - "sqlite4j"
    - "jdbc-sqlite"
    - "jdbc"
    - "sqlite"
  categories:
    - "data"
  status: "experimental"
  description: "Connect to the SQLite database via JDBC/DataSource/Hibernate"
