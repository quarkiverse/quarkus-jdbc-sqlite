---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: "JDBC Driver - Sqlite"
metadata:
  keywords:
    - "jdbc-sqlite"
    - "jdbc"
    - "sqlite"
  categories:
    - "data"
  status: "preview"
  description: "Connect to the Sqlite database via JDBC"